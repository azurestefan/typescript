"use strict";
//installed nodemon(allows rerun of node anytime file is changed), 
//concurrently(multiple script runs at the same time) modules.
//changed tsconfig.json rootDir, outDir
console.log(123);
var logSomething = function () {
    console.log('yoyo');
};
logSomething();
